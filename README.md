# Linked-List
Implementation LinkedList in java
contain functions get(Index), set(Index,value), addFirst(value), addLast(value), Insert(Index,value), deleteFirst(), deleteLast(),
delete(Index), IndexOf(value), size() and print().
